{
  "name": "js",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "start": "npm run serve",
    "serve": "ts-node main.js",
    "watch": "nodemon --watch src --exec 'ts-node' main.js",
    "watch:quiet": "nodemon --quiet --watch src --exec 'ts-node' main.js",
    "test": "mocha --require ts-node/register --watch-extensions ts,js 'test/**/*.test.{ts,js}'",
    "build": "tsc"
  },
  "author": "alan",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "mocha": "^6.2.0",
    "nodemon": "^1.19.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  },
  "dependencies": {
    "@alanchenchen/express-decorator": "^0.0.2"
  }
}
